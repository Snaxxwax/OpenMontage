# Provider Logs

Provider receipts and log artifacts that help validate reproducibility and safety.

- `schema/`: JSON Schemas for log/receipt artifacts.
- (future) `logs/`: optional committed examples or golden receipts (keep minimal).

These are channel-level because they enforce framework rules (e.g., provider-block receipts, chunked TTS receipts), not episode content.

