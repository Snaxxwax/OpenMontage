# Templates

Shared layout templates for Asymmetric.

- HyperFrames templates live in `hyperframes/` (preferred).
- Thumbnail templates live in `thumbnails/`.

Episode-specific templates belong in the episode workspace (e.g. `projects/<id>/assets/...`), not here.

