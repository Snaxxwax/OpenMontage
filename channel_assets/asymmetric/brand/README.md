# Brand

Canonical brand definitions for Asymmetric.

- Colors and semantic meaning live in `tokens.json`.
- Put logos in `logo/` (prefer SVG).
- Put typography notes/fonts in `typography/`.

Keep this folder stable: downstream templates should treat these paths as durable.

