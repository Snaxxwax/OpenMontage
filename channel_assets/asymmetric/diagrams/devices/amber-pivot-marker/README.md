# amber-pivot-marker

Marks the leverage pivot/control surface in a diagram.

- Spec: `device.v1.spec.json`
- Template: deferred (spec-first). Implement as HyperFrames SVG/CSS partial when first used.

