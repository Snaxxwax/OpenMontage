# Diagrams

Diagram-native assets for Asymmetric.

- `primitives/`: basic SVG building blocks (nodes, connectors, arrows, grids).
- `devices/`: higher-level recurring visual devices (chokepoints, pivots, x-ray reveals).

Prefer SVG with clean IDs/classes so HyperFrames can animate them predictably.

