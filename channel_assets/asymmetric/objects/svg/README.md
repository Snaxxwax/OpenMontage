# SVG Objects

Place reusable SVG objects here (icons, equipment silhouettes, generic shapes).

## Requirements
- Use viewBox, avoid hard-coded width/height where possible.
- Use CSS variables for colors when appropriate (see `channel_assets/asymmetric/brand/tokens.json`).
- Keep layers named (IDs/classes) so animations can target them.

